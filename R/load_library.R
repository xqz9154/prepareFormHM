# load_library.R
# install.packages("pacman")
library(pacman)
p_load(shiny,
       shinyFiles,
       zip,
       tidyverse,
       tools,
       elevatr,
       terra,
       sf,
       whitebox,
       tmap,
       stars,
       leaflet,
       leaflet.extras)
