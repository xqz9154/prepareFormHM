# load_library.R
# install.packages("pacman")
# install.packages("whitebox")
library(pacman)
p_load(shiny,
       shinyFiles,
       zip,
       tidyverse,
       tools,
       elevatr,
       terra,
       raster,
       sf,
       whitebox,
       leaflet,
       leaflet.extras)
